# Local: Vite proxies /api → backend (see vite.config.ts). You can leave unset and use proxy, or set:
VITE_API_URL="http://localhost:3001"

# Production (Vercel): set in Project Settings → Environment Variables — backend origin only, no /api path:
# VITE_API_URL=https://your-service.onrender.com

